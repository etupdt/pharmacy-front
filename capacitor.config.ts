import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'www.etupdt.fr',
  appName: 'mon-project',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
