import { IsUpdatedPipe } from './is-updated.pipe';

describe('IsUpdatedPipe', () => {
  it('create an instance', () => {
    const pipe = new IsUpdatedPipe();
    expect(pipe).toBeTruthy();
  });
});
