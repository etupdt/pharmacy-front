import { IsClientDirective } from './is-client.directive';

describe('IsClientDirective', () => {
  it('should create an instance', () => {
    const directive = new IsClientDirective();
    expect(directive).toBeTruthy();
  });
});
