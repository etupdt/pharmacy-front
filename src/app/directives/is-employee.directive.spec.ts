import { IsEmployeeDirective } from './is-employee.directive';

describe('IsEmployeeDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEmployeeDirective();
    expect(directive).toBeTruthy();
  });
});
